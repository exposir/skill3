# Darwin: 自进化 Skill 编排系统

![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Platform](https://img.shields.io/badge/platform-Claude%20Code-purple.svg)
![Status](https://img.shields.io/badge/status-active-success.svg)

一个基于 Claude Code skill 热重载机制的**自进化 AI Agent 框架**，通过元编程实现 skill 的自动生成、扩展和图结构编排。

> **重要说明**: Claude Code 的 Skill 是通过**自然语言描述自动触发**的，而不是通过 `/command` 斜杠命令调用。当你的请求匹配某个 Skill 的描述时，Claude 会自动加载并使用该 Skill。

---

## 目录

- [前置条件](#前置条件)
- [快速索引](#快速索引)
- [系统架构](#系统架构)
- [快速开始](#快速开始)
- [核心概念](#核心概念)
- [前端开发扩展](#前端开发扩展)
- [限制与边界](#限制与边界)
- [FAQ](#faq)
- [路线图](#路线图)

---

## 前置条件

### 环境要求

| 要求        | 版本/说明                    |
| ----------- | ---------------------------- |
| Claude Code | 最新版本 (支持 skill 热重载) |
| 操作系统    | macOS / Linux / Windows      |
| Node.js     | 18+ (前端扩展需要)           |
| Git         | 用于版本控制                 |

### 安装步骤

```bash
# 1. 克隆或复制项目到目标目录
git clone <repo-url> my-skill-project
cd my-skill-project

# 2. 确保目录结构完整
ls -la
# 应该看到: _meta/, _core/, _generated/, context/, skills.json

# 3. 在 Claude Code 中打开项目
claude .

# 4. 验证系统可用
/registry action=list
```

### 项目结构验证

```bash
# 必需的目录和文件
_meta/genesis.md        # ✅ 必需
_meta/registry.md       # ✅ 必需
_meta/orchestrator.md   # ✅ 必需
_core/skill-template.md # ✅ 必需
_core/communication.md  # ✅ 必需
context/state.json      # ✅ 必需
skills.json             # ✅ 必需
```

---

## 快速索引

### 核心功能速查

| 功能 | 触发方式（自然语言） | 示例 |
| ---- | -------------------- | ---- |
| 生成新 skill | 描述创建 skill 的需求 | `创建一个数据获取 skill` |
| 管理注册表 | 描述查看或管理 skill 的需求 | `列出所有已注册的 skills` |
| 编排执行 | 描述要执行的任务 | `处理数据并导出为 CSV` |

### 前端扩展功能

| 功能 | 触发方式（自然语言） | 示例 |
| ---- | -------------------- | ---- |
| 初始化项目 | 描述创建项目的需求 | `初始化一个 React 项目` |
| 生成模块 | 描述创建模块的需求 | `创建 auth 功能模块` |
| 协调开发 | 描述开发任务 | `规划实现用户登录功能` |
| 构建验证 | 描述构建需求 | `运行完整构建验证` |
| 契约管理 | 描述契约相关需求 | `验证接口契约一致性` |

### 常用操作

```
# 创建新 skill
帮我创建一个从 REST API 获取 JSON 数据的 skill

# 查看所有 skills
列出所有已注册的 skills

# 查看依赖图
显示 skill 的依赖关系图

# 执行 skill 管道
从 GitHub 获取用户数据并转换为 CSV

# 验证注册表一致性
验证注册表和文件是否一致
```

---

## 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户层 (User Layer)                             │
│                                                                             │
│    /genesis "需求"      /registry action=xxx      /orchestrate task="任务"   │
└──────────┬─────────────────────┬─────────────────────────┬──────────────────┘
           │                     │                         │
           ▼                     ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           元 Skill 层 (Meta Layer)                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐  │
│  │                 │  │                 │  │                             │  │
│  │    Genesis      │─▶│    Registry     │─▶│       Orchestrator          │  │
│  │   (代码生成器)   │  │   (注册表管理)   │  │       (执行编排器)           │  │
│  │                 │  │                 │  │                             │  │
│  └────────┬────────┘  └────────┬────────┘  └──────────────┬──────────────┘  │
│           │                    │                          │                 │
│           │  生成 skill 文件    │  维护 DAG 注册表          │  按拓扑序执行     │
│           ▼                    ▼                          ▼                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          基础设施层 (Infrastructure)                         │
│                                                                             │
│  ┌──────────────────────┐    ┌──────────────────────┐    ┌───────────────┐  │
│  │    skills.json       │    │  context/state.json  │    │  skill-*.md   │  │
│  │    (DAG 注册表)       │◀──▶│    (运行时状态)       │◀──▶│  (skill 文件)  │  │
│  └──────────────────────┘    └──────────────────────┘    └───────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         生成 Skill 层 (Generated Skills)                     │
│                                                                             │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│   │  skill-001  │───▶│  skill-002  │───▶│  skill-003  │───▶│  skill-004  │  │
│   │  api-fetch  │    │  transform  │    │  validate   │    │   output    │  │
│   └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘  │
│         │                  │                  │                  │          │
│         └──────────────────┴──────────────────┴──────────────────┘          │
│                              DAG 依赖关系                                    │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 数据流图

```
┌────────────────────────────────────────────────────────────────────────────┐
│                              执行流程                                       │
└────────────────────────────────────────────────────────────────────────────┘

  用户输入                    系统处理                      输出结果
     │                          │                            │
     ▼                          ▼                            ▼
┌─────────┐              ┌─────────────┐              ┌─────────────┐
│  需求   │──────────────▶│   Genesis   │──────────────▶│ 新 Skill 文件│
│  描述   │              │   解析生成   │              │  热重载生效  │
└─────────┘              └─────────────┘              └─────────────┘

┌─────────┐              ┌─────────────┐              ┌─────────────┐
│  任务   │──────────────▶│ Orchestrator│──────────────▶│  执行结果   │
│  描述   │              │  编排执行    │              │  状态更新   │
└─────────┘              └─────────────┘              └─────────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │   Skill 执行管道     │
                    │                     │
                    │  skill-a ──▶ skill-b│
                    │      │              │
                    │      ▼              │
                    │  skill-c ──▶ skill-d│
                    │                     │
                    └─────────────────────┘
```

### 热重载机制

```
┌─────────────────────────────────────────────────────────────────┐
│                        热重载工作流程                            │
└─────────────────────────────────────────────────────────────────┘

  1. Genesis 生成           2. 文件写入            3. 自动检测
     ┌─────────┐              ┌─────────┐           ┌─────────┐
     │ 解析需求 │──────────────▶│ 写入文件 │───────────▶│ 热重载  │
     │ 生成代码 │              │_generated│           │ 检测变化│
     └─────────┘              └─────────┘           └────┬────┘
                                                        │
  6. 可调用                 5. 更新注册表           4. 加载 Skill
     ┌─────────┐              ┌─────────┐           ┌────┴────┐
     │ /my-skill│◀─────────────│ skills  │◀──────────│ 解析元数据│
     │ 立即可用 │              │  .json  │           │ 加载指令  │
     └─────────┘              └─────────┘           └─────────┘
```

---

## 快速开始

### 1. 生成新 Skill

用自然语言描述你的需求，Claude 会自动匹配 Genesis skill：

```
帮我创建一个从 REST API 获取 JSON 数据的 skill
```

Genesis 会自动：

- 分析你的需求
- 生成符合规范的 skill 文件到 `_generated/` 目录
- 在 `.claude/skills/` 目录创建符号链接配置
- 更新 `skills.json` 注册表
- 热重载后立即可用

**输出示例：**
```
✓ 已创建 skill: skill-002-api-fetch
✓ 源文件: ./_generated/skill-002-api-fetch.md
✓ Claude Code 配置: ./.claude/skills/api-fetch/SKILL.md
✓ 已注册到 skills.json
✓ skill 已生效，用自然语言描述需求即可触发
```

### 2. 查看已有 Skills

用自然语言请求查看 skill 信息，Claude 会自动匹配 Registry skill：

```
列出所有已注册的 skills      # 列出所有 skills
显示 skill 的依赖关系图       # 显示依赖关系图
验证注册表是否一致            # 验证注册表一致性
```

### 3. 执行 Skill 管道

用自然语言描述任务，Claude 会自动匹配 Orchestrator skill 按 DAG 顺序执行：

```
从 GitHub 获取数据并转换为 CSV
```

Orchestrator 会自动：

- 分析任务，匹配相关 skills
- 按依赖关系排序执行
- 管理 skills 之间的数据传递

---

## 目录结构

```
darwin/
│
├── .claude/skills/              # Claude Code Skill 配置目录
│   │
│   ├── genesis/SKILL.md         # 🧬 创世技能
│   │   └── 功能: 根据用户需求自动生成新 skill
│   │
│   ├── registry/SKILL.md        # 📋 注册表管理
│   │   └── 功能: 管理 skill 注册、查看 DAG 图
│   │
│   ├── orchestrator/SKILL.md    # 🎭 执行编排器
│   │   └── 功能: 按 DAG 拓扑顺序执行 skill 管道
│   │
│   └── frontend/                # 前端扩展 Skills
│       ├── project-init/SKILL.md
│       ├── module-gen/SKILL.md
│       ├── contracts/SKILL.md
│       ├── build/SKILL.md
│       └── coordinate/SKILL.md
│
├── _meta/                       # 元 Skill 源文件
│   ├── genesis.md
│   ├── registry.md
│   ├── orchestrator.md
│   └── frontend/
│
├── _core/                       # 核心基础设施文档
│   ├── skill-template.md        # 📝 Skill 模板规范
│   └── communication.md         # 📡 通信协议
│
├── _generated/                  # 自动生成的 Skills (热重载目录)
│   └── skill-001-hello-world.md # 示例 skill
│
├── context/                     # 运行时上下文
│   └── state.json               # 📊 状态管理
│
├── skills.json                  # 📚 DAG 注册表
│
└── README.md                    # 📖 本文档
```

---

## 核心概念

### 1. Skill 是什么？

Skill 是一个 `.md` 文件，包含：

```
┌─────────────────────────────────────────┐
│              Skill 文件结构              │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │         YAML 元数据头              │  │
│  │  - id, name, version              │  │
│  │  - description                    │  │
│  │  - upstream / downstream          │  │
│  │  - inputs / outputs               │  │
│  │  - tags                           │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │         Instructions              │  │
│  │  Claude 执行这个 skill 时          │  │
│  │  应该做什么的具体步骤              │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │         Communication             │  │
│  │  如何读取上游数据                  │  │
│  │  如何输出给下游                    │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │         Error Handling            │  │
│  │  错误处理策略                      │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 2. DAG (有向无环图)

Skills 之间通过 `upstream` 和 `downstream` 形成依赖关系：

```
                    ┌─────────────────────────────────────────┐
                    │           DAG 依赖关系示例               │
                    └─────────────────────────────────────────┘

                              ┌───────────┐
                              │  Genesis  │
                              │  (元技能)  │
                              └─────┬─────┘
                                    │
                                    ▼
                              ┌───────────┐
                              │ Registry  │
                              │  (元技能)  │
                              └─────┬─────┘
                                    │
                                    ▼
                              ┌───────────┐
                              │Orchestrator│
                              │  (元技能)  │
                              └─────┬─────┘
                                    │
           ┌────────────────────────┼────────────────────────┐
           │                        │                        │
           ▼                        ▼                        ▼
    ┌─────────────┐          ┌─────────────┐          ┌─────────────┐
    │  skill-001  │          │  skill-002  │          │  skill-003  │
    │  api-fetch  │          │  db-query   │          │  file-read  │
    └──────┬──────┘          └──────┬──────┘          └──────┬──────┘
           │                        │                        │
           └────────────┬───────────┘                        │
                        │                                    │
                        ▼                                    │
                 ┌─────────────┐                             │
                 │  skill-004  │                             │
                 │  transform  │◀────────────────────────────┘
                 └──────┬──────┘
                        │
                        ▼
                 ┌─────────────┐
                 │  skill-005  │
                 │   output    │
                 └─────────────┘

    执行顺序: skill-001, skill-002, skill-003 (可并行)
              → skill-004 (等待上游完成)
              → skill-005
```

**关键规则：**

- `skill-004` 的 upstream 是 `[skill-001, skill-002, skill-003]`
- `skill-004` 必须等待所有上游 skills 完成后才执行
- `skill-004` 可以读取所有上游 skills 的输出数据
- 没有依赖关系的 skills 可以并行执行

### 3. 热重载机制

```
┌──────────────────────────────────────────────────────────────────┐
│                         热重载时序图                              │
└──────────────────────────────────────────────────────────────────┘

    时间 ─────────────────────────────────────────────────────────▶

    T1: 用户调用 /genesis
         │
         ▼
    ┌─────────────┐
    │ Genesis 解析 │
    │ 生成 skill  │
    └──────┬──────┘
           │
    T2:    ▼ 写入文件
    ┌─────────────────────────────────────┐
    │ _generated/skill-002-new-skill.md  │
    └─────────────────────────────────────┘
           │
    T3:    ▼ Claude Code 检测到文件变化
    ┌─────────────┐
    │  热重载触发  │
    │  加载新 skill│
    └──────┬──────┘
           │
    T4:    ▼ 更新注册表
    ┌─────────────┐
    │ skills.json │
    │ 添加新节点   │
    └──────┬──────┘
           │
    T5:    ▼ 立即可用
    ┌─────────────┐
    │ /new-skill  │
    │  可以调用了  │
    └─────────────┘

    整个过程: < 1 秒
```

---

## Skill 文件格式

### 完整模板

```markdown
---
# ==================== 基础信息 ====================
id: skill-001-my-skill # 唯一标识符
name: my-skill # 人类可读名称
version: 1.0.0 # 语义化版本号
description: 这个 skill 做什么 # 简短描述
directory: _generated/ # 所在目录

# ==================== 依赖关系 ====================
upstream: # 上游依赖 (此 skill 需要的输入来源)
  - skill-000-init
downstream: # 下游消费者 (使用此 skill 输出的 skills)
  - skill-002-output

# ==================== 输入输出 ====================
inputs: # 输入参数定义
  - name: url
    type: string # string | number | boolean | object | array
    required: true
    description: API 地址
  - name: headers
    type: object
    required: false
    description: 请求头

outputs: # 输出定义
  - name: data
    type: object
    description: 返回的 JSON 数据
  - name: status_code
    type: number
    description: HTTP 状态码

# ==================== 元信息 ====================
created_by: genesis # genesis | manual
created_at: 2026-01-14
updated_at: 2026-01-14
tags: # 分类标签
  - api
  - fetch
  - http
---

# My Skill

> 一句话描述这个 skill 的功能

## Context

背景信息，帮助 Claude 理解执行上下文。

## Instructions

当用户调用这个 skill 时，执行以下步骤：

### Step 1: 读取输入

从用户参数或上游 skill 获取输入数据。

### Step 2: 执行核心逻辑

具体的操作步骤...

### Step 3: 写入状态

将结果写入 state.json。

### Step 4: 返回结果

向用户展示结果。

## Communication

### Reading State
```

从 ./context/state.json 读取
访问 data.<upstream-skill-id>.output

````

### Writing State
```json
{
  "data": {
    "skill-001-my-skill": {
      "status": "completed",
      "output": { ... }
    }
  }
}
````

## Error Handling

- 如果输入无效: 返回错误信息
- 如果上游失败: 跳过执行或使用默认值

## Examples

### Example 1: 基本用法

输入: ...
输出: ...

````

---

## 状态管理详解

### state.json 结构

```json
{
  "version": "1.0.0",
  "current_execution": "exec-20260114-abc123",

  "executions": {
    "exec-20260114-abc123": {
      "execution_id": "exec-20260114-abc123",
      "started_at": "2026-01-14T10:00:00Z",
      "completed_at": null,
      "status": "running",
      "task": "处理 API 数据",

      "pipeline": ["skill-001", "skill-002", "skill-003"],
      "current_skill": "skill-002",

      "parallel_groups": [
        ["skill-001"],
        ["skill-002", "skill-003"]
      ],

      "data": {
        "skill-001": {
          "status": "completed",
          "started_at": "2026-01-14T10:00:01Z",
          "completed_at": "2026-01-14T10:00:05Z",
          "input": {
            "url": "https://api.example.com/data"
          },
          "output": {
            "success": true,
            "data": { "items": [...] },
            "metadata": { "count": 100 }
          },
          "error": null
        },

        "skill-002": {
          "status": "running",
          "started_at": "2026-01-14T10:00:06Z",
          "input": {
            "data": "<<ref:skill-001.output.data>>"
          },
          "output": null,
          "error": null
        },

        "skill-003": {
          "status": "pending",
          "input": null,
          "output": null,
          "error": null
        }
      },

      "metadata": {
        "triggered_by": "user",
        "context": {}
      }
    }
  }
}
````

### 状态流转图

```
┌─────────────────────────────────────────────────────────────────┐
│                      Skill 状态流转                              │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────┐
                    │ pending │ ◀─── 初始状态
                    └────┬────┘
                         │
         Orchestrator 开始执行
                         │
                         ▼
                    ┌─────────┐
           ┌────────│ running │────────┐
           │        └────┬────┘        │
           │             │             │
      执行失败        执行成功      上游失败
           │             │             │
           ▼             ▼             ▼
      ┌────────┐   ┌───────────┐   ┌─────────┐
      │ failed │   │ completed │   │ skipped │
      └────────┘   └───────────┘   └─────────┘
           │
           │ 如果配置了重试
           ▼
      ┌─────────┐
      │ retrying│───────▶ running
      └─────────┘
```

---

## 通信协议

### 1. 状态共享模式 (State-based)

Skills 通过 `state.json` 共享数据：

```
┌──────────────┐     写入      ┌──────────────┐     读取      ┌──────────────┐
│   Skill A    │──────────────▶│  state.json  │──────────────▶│   Skill B    │
│   (上游)      │              │              │              │   (下游)      │
└──────────────┘              └──────────────┘              └──────────────┘
       │                                                           │
       │  output: { data: [...] }                                 │
       │                                      input: <<ref:skill-a.output.data>>
       │                                                           │
       └───────────────────────────────────────────────────────────┘
                            数据流向
```

### 2. 引用语法

```
<<ref:skill-id.output.field>>        # 引用特定字段
<<ref:skill-id.output>>              # 引用整个输出
<<ref:skill-id.status>>              # 引用状态
```

### 3. 标准输出格式

```json
{
  "success": true,
  "data": {
    "items": [...],
    "total": 100
  },
  "metadata": {
    "processed_at": "2026-01-14T10:00:00Z",
    "duration_ms": 1500,
    "source": "api"
  },
  "errors": []
}
```

---

## 元 Skill 详解

### Genesis (创世技能)

```
┌─────────────────────────────────────────────────────────────────┐
│                        Genesis 工作流程                          │
└─────────────────────────────────────────────────────────────────┘

    输入: "帮我创建一个数据清洗 skill"

         │
         ▼
    ┌─────────────┐
    │  1. 解析需求 │
    │  提取关键信息│
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │ 2. 生成 ID  │
    │ skill-002-  │
    │ data-clean  │
    └──────┬──────┘
           │
           ▼
    ┌─────────────────────────────────┐
    │ 3. 创建文件（两个位置）           │
    │                                 │
    │  a. _generated/skill-002-*.md   │
    │     (源文件，用于版本控制)        │
    │                                 │
    │  b. .claude/skills/data-clean/  │
    │     SKILL.md (符号链接)          │
    │     (让 Claude Code 识别)        │
    └──────┬──────────────────────────┘
           │
           ▼
    ┌─────────────┐
    │ 4. 更新注册表│
    │ skills.json │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │ 5. 确认创建 │
    │ skill 已生效│
    └─────────────┘
```

### Registry (注册表管理)

| 触发方式 | 功能 |
| -------- | ---- |
| `列出所有 skills` | 列出所有已注册的 skills |
| `显示依赖关系图` | 显示 DAG 依赖关系图 |
| `验证注册表一致性` | 验证注册表和文件一致性 |
| `添加 xxx skill` | 手动添加 skill |
| `移除 xxx skill` | 移除 skill |
| `更新 xxx skill 元数据` | 更新 skill 元数据 |

### Orchestrator (执行编排器)

```
┌─────────────────────────────────────────────────────────────────┐
│                     Orchestrator 执行流程                        │
└─────────────────────────────────────────────────────────────────┘

    输入: /orchestrate task="处理数据" skills=["a", "b", "c"]

         │
         ▼
    ┌───────────────────────────────────────────────────────────┐
    │ Step 1: 初始化执行                                         │
    │ - 生成 execution_id                                       │
    │ - 创建执行记录到 state.json                                │
    └─────────────────────────────┬─────────────────────────────┘
                                  │
                                  ▼
    ┌───────────────────────────────────────────────────────────┐
    │ Step 2: 确定执行管道                                       │
    │ - 验证 skills 存在                                        │
    │ - 拓扑排序                                                │
    │ - 识别可并行的 skills                                      │
    └─────────────────────────────┬─────────────────────────────┘
                                  │
                                  ▼
    ┌───────────────────────────────────────────────────────────┐
    │ Step 3: 执行循环                                          │
    │                                                           │
    │   for each skill in pipeline:                             │
    │       1. 更新 current_skill                               │
    │       2. 读取 skill 文件                                  │
    │       3. 准备输入 (从上游 output)                          │
    │       4. 执行 Instructions                                │
    │       5. 写入输出到 state.json                            │
    │       6. 更新状态                                         │
    │                                                           │
    └─────────────────────────────┬─────────────────────────────┘
                                  │
                                  ▼
    ┌───────────────────────────────────────────────────────────┐
    │ Step 4: 完成执行                                          │
    │ - 更新执行状态为 completed                                 │
    │ - 收集所有输出                                            │
    │ - 报告结果给用户                                          │
    └───────────────────────────────────────────────────────────┘
```

---

## 完整示例

### 示例 1: 创建数据处理管道

```bash
# 1. 创建 API 获取 skill
/genesis "创建 api-fetch skill，从 URL 获取 JSON 数据，支持 GET 请求"

# Genesis 输出:
# ✓ 已创建 skill: skill-002-api-fetch
# ✓ 路径: ./_generated/skill-002-api-fetch.md
# ✓ 已注册到 skills.json

# 2. 创建数据转换 skill (依赖 api-fetch)
/genesis "创建 json-to-csv skill，将 JSON 转换为 CSV 格式，上游是 api-fetch"

# Genesis 输出:
# ✓ 已创建 skill: skill-003-json-to-csv
# ✓ upstream: [skill-002-api-fetch]
# ✓ 已更新 DAG

# 3. 查看依赖图
/registry action=graph

# 输出:
# genesis
#   └── registry
#         └── orchestrator
#               └── skill-002-api-fetch
#                     └── skill-003-json-to-csv

# 4. 执行管道
/orchestrate task="获取 GitHub repos 并导出为 CSV" input={"url": "https://api.github.com/users/octocat/repos"}

# 执行输出:
# === Execution: exec-20260114-xyz789 ===
# [1/2] ▶ api-fetch: Running...
# [1/2] ✓ api-fetch: Completed (1.2s)
# [2/2] ▶ json-to-csv: Running...
# [2/2] ✓ json-to-csv: Completed (0.3s)
# === Complete ===
# CSV 文件已生成
```

### 示例 2: 并行执行

```bash
# 创建三个独立的数据源 skills
/genesis "创建 fetch-users skill，获取用户数据"
/genesis "创建 fetch-orders skill，获取订单数据"
/genesis "创建 fetch-products skill，获取产品数据"

# 创建聚合 skill (依赖上面三个)
/genesis "创建 aggregate skill，聚合用户、订单、产品数据，上游是 fetch-users, fetch-orders, fetch-products"

# 执行
/orchestrate task="聚合所有数据"

# 执行输出:
# === Execution: exec-20260114-agg001 ===
# [1/4] ▶ fetch-users: Running...     ┐
# [2/4] ▶ fetch-orders: Running...    ├── 并行执行
# [3/4] ▶ fetch-products: Running...  ┘
# [1/4] ✓ fetch-users: Completed (0.8s)
# [2/4] ✓ fetch-orders: Completed (1.1s)
# [3/4] ✓ fetch-products: Completed (0.9s)
# [4/4] ▶ aggregate: Running...       <── 等待上游完成
# [4/4] ✓ aggregate: Completed (0.2s)
# === Complete ===
```

---

## 自进化能力

```
┌─────────────────────────────────────────────────────────────────┐
│                        自进化循环                                │
└─────────────────────────────────────────────────────────────────┘

    ┌─────────────┐
    │  用户需求   │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐         ┌─────────────┐
    │   Genesis   │────────▶│  新 Skill   │
    │  生成 skill │         │    文件     │
    └──────┬──────┘         └──────┬──────┘
           │                       │
           │                       ▼
           │                ┌─────────────┐
           │                │   热重载    │
           │                │  立即可用   │
           │                └──────┬──────┘
           │                       │
           ▼                       ▼
    ┌─────────────┐         ┌─────────────┐
    │ 模式识别    │◀────────│  使用反馈   │
    │ 优化模板    │         │  执行数据   │
    └──────┬──────┘         └─────────────┘
           │
           │ 识别重复模式
           ▼
    ┌─────────────┐
    │ 自动创建    │
    │ 可复用 skill│
    └─────────────┘
```

### 进化方向

1. **模板优化**: 根据生成的 skills 使用反馈优化模板
2. **模式学习**: 识别常见 skill 模式，创建子模板
3. **自动标签**: 基于内容自动推断合适的标签
4. **依赖优化**: 自动识别可以并行的 skills

---

## 错误处理

### 错误类型和处理策略

| 错误类型     | 处理策略     | 说明                   |
| ------------ | ------------ | ---------------------- |
| Skill 不存在 | 报错         | 提示用户创建或检查拼写 |
| 上游失败     | skip / stop  | 根据配置决定是否继续   |
| 循环依赖     | 拒绝         | 注册时检测并拒绝       |
| 超时         | retry        | 默认重试 3 次          |
| 文件缺失     | 标记 missing | 在 validate 时报告     |

### 错误输出格式

```json
{
  "success": false,
  "data": null,
  "errors": [
    {
      "code": "UPSTREAM_FAILED",
      "message": "上游 skill 'api-fetch' 执行失败",
      "skill_id": "api-fetch",
      "details": "Connection timeout after 30s"
    }
  ]
}
```

---

## 文件说明

| 文件                      | 类型     | 作用         | 可否修改    |
| ------------------------- | -------- | ------------ | ----------- |
| `skills.json`             | 配置     | DAG 注册表   | 自动维护    |
| `context/state.json`      | 状态     | 运行时状态   | 自动维护    |
| `.claude/skills/*/SKILL.md` | Skill  | Skill 定义   | ✅ 可自定义 |
| `_meta/*.md`              | 元 Skill | Skill 源文件 | ❌ 不建议   |
| `_core/skill-template.md` | 文档     | 模板规范     | ✅ 可自定义 |
| `_core/communication.md`  | 文档     | 通信协议     | ✅ 可扩展   |
| `_generated/*.md`         | Skill    | 你的 skills  | ✅ 自由编辑 |

---

## 注意事项

1. **不要手动修改** `_meta/` 目录下的核心 skills 源文件
2. **自定义 skills** 应在 `.claude/skills/` 目录下创建目录和 `SKILL.md` 文件
3. **修改 skill 后** 会自动热重载，无需重启
4. **skills.json** 是注册表，由 Genesis 和 Registry 自动维护
5. **循环依赖** 会被自动检测并拒绝
6. **并行执行** 会自动识别无依赖关系的 skills
7. **Skill 触发** 是通过自然语言描述自动匹配的，而非斜杠命令

---

# 前端开发扩展

## 前端开发架构

系统已扩展支持**超大型前端项目开发**，通过模块化 skill 实现多模块并行开发。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           前端开发 Skill 架构                                │
└─────────────────────────────────────────────────────────────────────────────┘

                              用户需求
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        前端元 Skill 层                                       │
│                                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│  │ project-init│───▶│module-gen   │───▶│ contracts   │───▶│   build     │  │
│  │ (项目初始化) │    │ (模块生成)   │    │ (接口契约)   │    │ (构建验证)   │  │
│  └─────────────┘    └──────┬──────┘    └──────┬──────┘    └─────────────┘  │
│                            │                  │                             │
│                            └────────┬─────────┘                             │
│                                     ▼                                       │
│                            ┌─────────────┐                                  │
│                            │ coordinator │                                  │
│                            │ (模块协调器) │                                  │
│                            └─────────────┘                                  │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         模块 Skill 层                                        │
│                                                                             │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐ │
│  │skill-module-  │  │skill-module-  │  │skill-module-  │  │skill-module-  │ │
│  │    auth       │  │    api        │  │    user       │  │    ui         │ │
│  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘ │
│          │                  │                  │                  │         │
└──────────┼──────────────────┼──────────────────┼──────────────────┼─────────┘
           │                  │                  │                  │
           ▼                  ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          代码层 (src/)                                       │
│                                                                             │
│  src/modules/auth/    src/modules/api/    src/modules/user/   src/modules/ui│
│  src/contracts/auth.contract.ts  api.contract.ts  user.contract.ts  ...     │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 前端开发快速开始

### 1. 初始化项目

```
初始化一个名为 my-enterprise-app 的 React 项目
```

生成：

- 项目目录结构
- package.json, tsconfig.json, vite.config.ts
- 模块注册表

### 2. 创建业务模块

```
创建 auth 功能模块
创建 api 核心模块
创建 user 功能模块，依赖 auth 和 api
```

每个模块生成：

- `src/modules/<name>/` - 模块代码目录
- `src/contracts/<name>.contract.ts` - 接口契约
- `_generated/skill-module-<name>.md` - 模块维护 skill

### 3. 协调多模块开发

```
帮我规划实现用户登录功能
开始执行开发计划
```

协调器会：

- 分析任务涉及的模块
- 按依赖关系编排执行
- 并行执行无依赖模块
- 验证构建结果

### 4. 验证构建

```
运行完整的构建验证
```

执行：

- ESLint 代码检查
- TypeScript 类型检查
- 单元测试
- 构建验证

---

## 前端 Skill 功能参考

| 功能 | 触发方式 | 示例 |
| ---- | -------- | ---- |
| 初始化前端项目 | 描述项目需求 | `初始化一个 React 项目` |
| 生成业务模块 | 描述模块需求 | `创建 auth 功能模块` |
| 管理接口契约 | 描述契约需求 | `验证接口契约一致性` |
| 构建验证 | 描述构建需求 | `运行完整构建验证` |
| 多模块协调 | 描述开发任务 | `规划实现登录功能` |
| 模块维护 | 描述维护需求 | `在 auth 模块中实现登录表单` |

---

## 模块类型

| 类型      | 说明         | 示例                       |
| --------- | ------------ | -------------------------- |
| `feature` | 业务功能模块 | auth, user, product, order |
| `shared`  | 共享功能模块 | ui, form, table, modal     |
| `core`    | 核心基础模块 | api, store, router, i18n   |

---

## 模块目录结构

```
src/modules/<name>/
├── index.ts                    # 模块入口和导出
├── types.ts                    # 类型定义
├── constants.ts                # 常量
│
├── components/                 # React 组件
│   ├── index.ts
│   └── <Name>Container.tsx
│
├── hooks/                      # React Hooks
│   ├── index.ts
│   └── use<Name>.ts
│
├── services/                   # 业务服务
│   ├── index.ts
│   └── <name>.service.ts
│
├── store/                      # 状态管理
│   ├── index.ts
│   └── <name>.slice.ts
│
└── __tests__/                  # 测试
    └── <name>.test.ts
```

---

## 接口契约系统

契约文件定义模块间的接口规范：

```typescript
// src/contracts/auth.contract.ts

// 类型导出
export type { AuthState, AuthData } from "@modules/auth/types";

// 接口定义
export interface IAuthModule {
  getState(): AuthState;
  login(params: LoginParams): Promise<AuthData>;
  logout(): void;
}

// 导出清单
export const AUTH_EXPORTS = {
  components: ["AuthProvider", "AuthGuard"],
  hooks: ["useAuth"],
  services: ["authService"],
} as const;
```

其他模块通过契约引用：

```typescript
import { useAuth, AuthProvider } from "@modules/auth";
import type { AuthState } from "@contracts/auth.contract";
```

---

## 多模块开发工作流

```
┌─────────────────────────────────────────────────────────────────┐
│                     多模块开发流程                               │
└─────────────────────────────────────────────────────────────────┘

  1. 需求分析            2. 任务分解            3. 并行开发
     ┌─────────┐           ┌─────────┐           ┌─────────┐
     │ 用户需求 │──────────▶│ 模块拆分 │──────────▶│ 并行执行 │
     └─────────┘           └─────────┘           └─────────┘
                                                      │
                                               ┌──────┴──────┐
                                               ▼             ▼
                                          [auth skill]  [api skill]
                                               │             │
                                               └──────┬──────┘
                                                      ▼
  4. 契约验证            5. 集成测试            6. 构建发布
     ┌─────────┐           ┌─────────┐           ┌─────────┐
     │ 接口对齐 │◀──────────│ 功能验证 │◀──────────│ 生产构建 │
     └─────────┘           └─────────┘           └─────────┘
```

---

## 前端扩展目录结构

```
skill2/
├── _meta/
│   ├── genesis.md              # 核心: Skill 生成器
│   ├── registry.md             # 核心: 注册表管理
│   ├── orchestrator.md         # 核心: 执行编排
│   │
│   └── frontend/               # 前端扩展 Skills
│       ├── project-init.md     # 项目初始化
│       ├── module-generator.md # 模块生成器
│       ├── contracts.md        # 契约管理
│       ├── build.md            # 构建验证
│       └── coordinator.md      # 模块协调器
│
├── _generated/                 # 生成的模块 Skills
│   ├── skill-module-auth.md
│   ├── skill-module-api.md
│   └── ...
│
├── src/                        # 前端代码目录
│   ├── modules/               # 业务模块
│   │   ├── auth/
│   │   ├── api/
│   │   └── ...
│   ├── contracts/             # 接口契约
│   ├── shared/                # 共享代码
│   └── app/                   # 应用入口
│
├── templates/                  # 代码模板
│   └── module/
│       └── skill-template.md
│
└── skills.json                 # 注册表 (含前端扩展)
```

---

## 版本历史

| 版本  | 日期       | 变更                             |
| ----- | ---------- | -------------------------------- |
| 2.0.0 | 2026-01-14 | 添加前端开发扩展，支持超大型项目 |
| 1.0.0 | 2026-01-14 | 初始版本，包含核心三元 Skill     |

---

## 限制与边界

### 当前限制

| 限制             | 说明                             | 计划解决 |
| ---------------- | -------------------------------- | -------- |
| **单会话状态**   | state.json 在会话间不自动持久化  | v2.1     |
| **无实时协作**   | 不支持多用户同时编辑同一 skill   | v3.0     |
| **手动依赖声明** | 需要手动指定 upstream/downstream | v2.2     |
| **无回滚机制**   | skill 修改后无法自动回滚         | v2.1     |
| **单项目范围**   | skills 不能跨项目共享            | v3.0     |

### 不适用场景

- **实时系统**: 不适合需要毫秒级响应的场景
- **大规模数据处理**: 不适合处理 GB 级数据
- **生产环境部署**: 这是开发辅助工具，不是运行时框架
- **非 Claude Code 环境**: 依赖 Claude Code 的 skill 机制

### 最佳实践

```
✅ 适合                          ❌ 不适合
─────────────────────────────────────────────────
开发流程自动化                    实时数据处理
代码生成和重构                    大规模并发请求
模块化前端开发                    生产环境服务
任务编排和协调                    跨团队实时协作
原型快速迭代                      长期数据存储
```

---

## FAQ

### 基础问题

**Q: Skill 和普通脚本有什么区别？**

A: Skill 是 `.md` 文件，包含给 Claude 的**指令**而不是可执行代码。Claude 读取指令后执行相应操作。这使得 skill 可以用自然语言描述复杂逻辑。

**Q: 热重载是怎么工作的？**

A: 当 `.claude/skills/` 目录下的文件发生变化时，Claude Code 会自动检测并重新加载 skill 定义。整个过程不到 1 秒。

**Q: 可以手动创建 skill 吗？**

A: 可以。在 `.claude/skills/` 目录下创建一个目录（如 `my-skill/`），然后在其中创建 `SKILL.md` 文件，包含 `name` 和 `description` 字段的 YAML frontmatter。Skill 会自动被加载。

**Q: Skill 是如何被触发的？**

A: Claude Code 的 skill 是通过**自然语言描述自动触发**的。Claude 会根据你的请求匹配最相关的 skill，并在加载前询问你确认。

### 技术问题

**Q: 如何处理 skill 执行失败？**

A: 系统会在 `state.json` 中记录错误信息。Orchestrator 支持三种策略：

- `stop`: 停止整个管道
- `skip`: 跳过失败的 skill 继续执行
- `retry`: 重试失败的 skill（最多 3 次）

**Q: 如何调试 skill？**

A:

1. 查看 `context/state.json` 中的执行状态
2. 请求 Claude 验证注册表一致性
3. 检查 skill 文件的 YAML 元数据格式是否正确

**Q: 循环依赖怎么处理？**

A: 系统在注册时会自动检测循环依赖并拒绝。如果需要双向通信，建议：

1. 提取共享逻辑到独立 skill
2. 使用事件驱动模式
3. 重新设计依赖关系

### 前端扩展问题

**Q: 可以用于 Vue/Svelte 项目吗？**

A: 是的。在初始化项目时指定框架：

```
初始化一个 Vue 项目
初始化一个 Svelte 项目
```

**Q: 模块间如何通信？**

A: 通过接口契约（contracts）。每个模块导出 TypeScript 接口，其他模块通过契约引用，确保类型安全。

**Q: 如何处理模块冲突？**

A: 请求 Claude 解决模块冲突，系统会检测并提供解决方案。

---

## 路线图

### v2.1 (计划中)

- [ ] **状态持久化**: 支持 state.json 跨会话保持
- [ ] **回滚机制**: skill 修改历史和回滚
- [ ] **性能优化**: 大型 DAG 的拓扑排序优化
- [ ] **日志系统**: 详细的执行日志和追踪

### v2.2 (规划中)

- [ ] **自动依赖推断**: 基于代码分析自动识别依赖
- [ ] **Skill 测试框架**: 为 skills 编写和运行测试
- [ ] **可视化工具**: DAG 依赖关系可视化
- [ ] **模板库**: 常用 skill 模板库

### v3.0 (愿景)

- [ ] **多项目支持**: skills 跨项目共享和引用
- [ ] **团队协作**: 多用户同时开发支持
- [ ] **插件系统**: 第三方 skill 扩展
- [ ] **云端同步**: skill 定义云端备份

### 贡献指南

欢迎贡献！你可以：

1. **扩展 Skill**: 在 `_generated/` 中添加新的通用 skill
2. **改进模板**: 优化 `_core/skill-template.md`
3. **添加前端模块模板**: 扩展 `templates/` 目录
4. **报告问题**: 提交 issue 描述问题或建议

---

## License

MIT
