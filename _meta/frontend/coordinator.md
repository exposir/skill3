---
id: coordinator
name: Module Coordinator
version: 1.0.0
description: 协调多模块并行开发，管理模块间依赖和通信
directory: _meta/frontend/
upstream:
  - module-generator
  - contracts
downstream:
  - build
inputs:
  - name: action
    type: string
    required: true
    description: 操作类型 (plan/execute/status/resolve)
  - name: task
    type: string
    required: false
    description: 任务描述
  - name: modules
    type: array
    required: false
    description: 涉及的模块列表
outputs:
  - name: execution_plan
    type: object
    description: 执行计划
  - name: results
    type: object
    description: 执行结果
created_by: manual
created_at: 2026-01-14
updated_at: 2026-01-14
tags:
  - meta
  - frontend
  - coordinator
  - orchestration
---

# Module Coordinator - 模块协调器

> 协调多模块并行开发，管理模块间依赖、解决冲突、同步状态

## Context

模块协调器是大型前端项目开发的指挥中心，负责：
1. 分析任务并分解到各模块
2. 按依赖关系编排执行顺序
3. 协调模块间的接口变更
4. 解决模块间冲突
5. 同步开发状态

## Coordination Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           模块协调架构                                       │
└─────────────────────────────────────────────────────────────────────────────┘

                              用户需求
                                 │
                                 ▼
                    ┌─────────────────────────┐
                    │      Coordinator        │
                    │       (协调器)           │
                    └───────────┬─────────────┘
                                │
            ┌───────────────────┼───────────────────┐
            │                   │                   │
            ▼                   ▼                   ▼
    ┌───────────────┐   ┌───────────────┐   ┌───────────────┐
    │  Task Planner │   │ Dependency    │   │   Conflict    │
    │   (任务规划)   │   │  Resolver     │   │   Resolver    │
    │               │   │  (依赖解析)    │   │   (冲突解决)   │
    └───────┬───────┘   └───────┬───────┘   └───────┬───────┘
            │                   │                   │
            └───────────────────┼───────────────────┘
                                │
                                ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                        执行层                                        │
    │                                                                     │
    │   ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐      │
    │   │ Module  │     │ Module  │     │ Module  │     │ Module  │      │
    │   │ Skill A │────▶│ Skill B │────▶│ Skill C │────▶│ Skill D │      │
    │   └─────────┘     └─────────┘     └─────────┘     └─────────┘      │
    │                                                                     │
    │   并行执行无依赖的模块，串行执行有依赖的模块                           │
    └─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
                    ┌─────────────────────────┐
                    │      Build Validator    │
                    │       (构建验证)         │
                    └─────────────────────────┘
```

## Instructions

当用户调用 `/coordinate` 时，根据 action 参数执行相应操作：

### Action: plan

规划任务执行：

```
/coordinate action=plan task="实现用户登录功能"
```

1. 分析任务需求
2. 识别涉及的模块
3. 分析模块依赖
4. 生成执行计划

输出：
```
=== Execution Plan ===

Task: 实现用户登录功能

涉及模块:
  1. auth   - 认证核心逻辑
  2. api    - 登录 API 调用
  3. user   - 用户状态管理
  4. ui     - 登录表单组件

依赖分析:
  api    → (无依赖)
  auth   → api
  user   → auth, api
  ui     → auth, user

执行计划:
  ┌─────────────────────────────────────────────────────────┐
  │ Phase 1 (并行):                                         │
  │   [1] api   - 实现 POST /auth/login 接口调用            │
  │                                                         │
  │ Phase 2 (并行):                                         │
  │   [2] auth  - 实现 useAuth hook 和 AuthProvider         │
  │                                                         │
  │ Phase 3 (并行):                                         │
  │   [3] user  - 实现用户状态存储和 useUser hook           │
  │   [4] ui    - 实现 LoginForm 组件                       │
  │                                                         │
  │ Phase 4:                                                │
  │   [5] 集成测试和构建验证                                 │
  └─────────────────────────────────────────────────────────┘

预计工作量: 4 个模块 skill 执行
是否开始执行? [Y/n]
```

### Action: execute

执行协调计划：

```
/coordinate action=execute task="实现用户登录功能"
```

按计划执行各模块 skill：

```
=== Coordinated Execution ===

Task: 实现用户登录功能

[Phase 1]
  ▶ api: 实现登录接口...
    → 调用 /skill-module-api action=implement requirement="添加 POST /auth/login"
  ✓ api: 完成 (新增 authApi.login 方法)

[Phase 2]
  ▶ auth: 实现认证逻辑...
    → 调用 /skill-module-auth action=implement requirement="实现 useAuth 和 AuthProvider"
  ✓ auth: 完成 (新增 useAuth, AuthProvider, AuthGuard)

[Phase 3]
  ▶ user: 实现用户状态... (并行)
  ▶ ui: 实现登录表单... (并行)
  ✓ user: 完成 (新增 useUser, userStore)
  ✓ ui: 完成 (新增 LoginForm, LoginPage)

[Phase 4]
  ▶ 验证契约一致性...
  ✓ 所有契约验证通过
  ▶ 运行构建验证...
  ✓ 类型检查通过
  ✓ 测试通过 (12/12)
  ✓ 构建成功

═══════════════════════════════════════════════════════════
Execution: ✅ SUCCESS
Duration: 45s
Modules Modified: 4
Files Created: 12
Files Modified: 8
═══════════════════════════════════════════════════════════

变更摘要:
  api:
    + src/modules/api/endpoints/auth.ts
    ~ src/modules/api/index.ts

  auth:
    + src/modules/auth/hooks/useAuth.ts
    + src/modules/auth/components/AuthProvider.tsx
    + src/modules/auth/components/AuthGuard.tsx
    ~ src/modules/auth/index.ts

  user:
    + src/modules/user/store/user.slice.ts
    + src/modules/user/hooks/useUser.ts
    ~ src/modules/user/index.ts

  ui:
    + src/modules/ui/components/LoginForm.tsx
    + src/modules/ui/pages/LoginPage.tsx
    ~ src/modules/ui/index.ts
```

### Action: status

查看当前协调状态：

```
/coordinate action=status
```

输出：
```
=== Coordination Status ===

当前执行: exec-20260114-login

进度: ████████████░░░░ 75% (3/4 phases)

模块状态:
  ┌────────┬───────────┬──────────────┬─────────┐
  │ Module │ Status    │ Last Action  │ Time    │
  ├────────┼───────────┼──────────────┼─────────┤
  │ api    │ ✅ Done    │ implement    │ 10s ago │
  │ auth   │ ✅ Done    │ implement    │ 25s ago │
  │ user   │ ✅ Done    │ implement    │ 5s ago  │
  │ ui     │ 🔄 Running │ implement    │ now     │
  └────────┴───────────┴──────────────┴─────────┘

待处理:
  - Phase 4: 构建验证

冲突: 无
警告: 无
```

### Action: resolve

解决模块间冲突：

```
/coordinate action=resolve
```

检测并解决冲突：

```
=== Conflict Resolution ===

检测到冲突:

[Conflict 1] 接口不匹配
  位置: auth ↔ user
  描述: auth.AuthState.user 类型与 user.User 类型不一致
  auth: { user: User | null }
  user: { user: UserProfile | undefined }

  解决方案:
    A) 统一使用 User | null (推荐)
    B) 统一使用 UserProfile | undefined
    C) 创建共享类型定义

  选择 [A/B/C]: A

  ✓ 已更新 user 模块使用 User | null
  ✓ 已更新契约文件
  ✓ 类型检查通过

[Conflict 2] 循环依赖
  路径: auth → user → auth

  解决方案:
    A) 提取共享逻辑到 shared 模块 (推荐)
    B) 使用依赖注入打破循环
    C) 合并为单一模块

  选择 [A/B/C]: A

  ✓ 创建 shared/auth-types 模块
  ✓ 重构 auth 和 user 模块
  ✓ 依赖关系已修正

═══════════════════════════════════════════════════════════
Resolution: ✅ All conflicts resolved
═══════════════════════════════════════════════════════════
```

## Parallel Execution Strategy

```
┌─────────────────────────────────────────────────────────────────┐
│                      并行执行策略                                │
└─────────────────────────────────────────────────────────────────┘

依赖图:
              ┌─────┐
              │ api │
              └──┬──┘
                 │
        ┌────────┴────────┐
        ▼                 ▼
    ┌─────┐           ┌─────┐
    │auth │           │store│
    └──┬──┘           └──┬──┘
       │                 │
       └────────┬────────┘
                ▼
            ┌─────┐
            │user │
            └──┬──┘
               │
        ┌──────┴──────┐
        ▼             ▼
    ┌─────┐       ┌─────┐
    │ ui  │       │dashboard│
    └─────┘       └─────┘

执行分层:
  Layer 0: [api]           ← 无依赖，首先执行
  Layer 1: [auth, store]   ← 依赖 api，可并行
  Layer 2: [user]          ← 依赖 auth, store
  Layer 3: [ui, dashboard] ← 依赖 user，可并行
```

## Communication

### Writing State

```json
{
  "data": {
    "coordinator": {
      "status": "running",
      "output": {
        "execution_id": "exec-20260114-login",
        "task": "实现用户登录功能",
        "phases": [
          { "phase": 1, "modules": ["api"], "status": "completed" },
          { "phase": 2, "modules": ["auth"], "status": "completed" },
          { "phase": 3, "modules": ["user", "ui"], "status": "running" },
          { "phase": 4, "modules": ["build"], "status": "pending" }
        ],
        "current_phase": 3,
        "progress": 75
      }
    }
  }
}
```

## Error Handling

- **模块 skill 不存在**: 自动调用 module-generator 创建
- **依赖未满足**: 等待依赖模块完成
- **执行超时**: 报告并提供重试选项
- **冲突无法自动解决**: 交由用户决策

## Examples

### Example 1: 规划新功能

```
/coordinate action=plan task="添加购物车功能"
```

### Example 2: 执行开发任务

```
/coordinate action=execute task="实现用户注册流程" modules=["auth", "user", "api", "ui"]
```

### Example 3: 查看进度

```
/coordinate action=status
```

### Example 4: 解决冲突

```
/coordinate action=resolve
```
